[pytest]
testpaths = tests
# Tests import backend modules by their top-level names (main, agent.*, tools.*),
# so the backend directory itself has to be importable.
pythonpath = .
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
